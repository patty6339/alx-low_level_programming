#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - check the code
 *
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
    // Corrected the syntax issue
    // Declaring a variable i is not necessary for this example
    printf("%d\n", _strlen("My Dyn Lib"));
    return (EXIT_SUCCESS);
}

